# ===== 闭源算法引擎（源码与二进制均不进公开仓）=====
# 源码：本地开发/自用时存在，编译成二进制后分发，绝不提交
engine_core.py
# 编译产物（Nuitka 原生编译出，Linux 平台分发用）
sm_dsp_engine
sm_dsp_engine_*
*.bin

# Nuitka 本地构建残留（不进仓）
engine_core.build/
engine_core.dist/
engine_core.onefile-build/
.build-venv/

# ===== Python 杂物 =====
__pycache__/
*.pyc
*.pyo

# ===== 本地开发 / 废弃产物（不对外）=====
_mocktest.py
_mocktest.sh
_obsolete_*/

# ===== 用户本地配置（含 NAS 路径、密钥等，不对外）=====
*.local
.env

# ===== 非 Jellyfin 产品 / 仍含公式副本，不进公开仓 =====
# 这些是桌面 MPV 版、旧控制台等独立产品，不要随 Jellyfin 仓发布：
peq_toggle.py
spacemaster_console.py
peq_control.html
spacemaster-mpv-plugin/

# ===== 根目录旧厚壳 wrapper（含 pan/adelay 公式，已被 docker-jellyfin/sm_wrapper.sh 薄壳取代）=====
# 注意：只忽略根目录这一份，不影响已提交的 docker-jellyfin/sm_wrapper.sh 薄壳
/sm_wrapper.sh
# 旧废弃目录 / 缓存
_obsolete_*/
__pycache__/
